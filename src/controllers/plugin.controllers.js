
const pluginPathMap= {
    googleSheets: require('../functions/googleSheets')
}

const pluginsController= async(req, res) => {
    try {
        
    } catch (error) {
        
    }
}

module.exports= {
    pluginsController,
}