module.exports= {
    pluginsController: require('./plugin.controllers')['pluginsController'],
    createFormController: require('./form.controllers')['createForm'],
    fillFormController: require('./form.controllers')['fillForm'],
}